version: 2

models:
  - name: dim_actor
    columns:
      - name: actor_key
        description: The surrogate key of the actor
        tests:
          - not_null
          - unique
      - name: film_key
        description: The surrogate key of the film
        tests:
          - not_null
      - name: first_name
        description: The first name of actor
      - name: last_name
        description: The last name of actor