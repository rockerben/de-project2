version: 2

seeds:
  - name: address
    config:

      column_types:
        district: varchar
        address: varchar
        address2: varchar
        address_ID: integer
        postal_code: integer
        phone: integer
        last_update: timestamp
        city_id: integer

