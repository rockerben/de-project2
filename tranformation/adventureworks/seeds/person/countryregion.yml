version: 2

seeds:
  - name: countryregion
    config:
      database: adventureworks
      schema: person
      column_types:
        countryregioncode: varchar
        modifieddate: timestamp
        name: varchar
